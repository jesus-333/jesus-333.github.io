% =====================================================================
%  LOCAL OVERRIDES -- hand-written, deliberately outside the single
%  source of truth. Ships empty and should normally stay empty.
%
%  Use it only for a genuine one-off that must not reach the website:
%  a \renewcommand for one application, a restated entry, an extra
%  \vspace to win back a line.
%
%  NEVER COMMIT TAILORED CONTENT HERE. Tailoring belongs in
%  common_material/cv.toml under [profiles.<name>]; that way the choice
%  is reusable and reviewable. Anything in this file is invisible to
%  every other output and will quietly rot.
% =====================================================================
